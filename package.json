{
  "devDependencies": {
    "cypress": "^13.9.0",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "open": "cypress open --e2e --browser chrome"
  },
  "dependencies": {
    "moment": "^2.30.1"
  }
}
